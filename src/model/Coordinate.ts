export interface Coordinate {
  x: number;
  y: number;
}
export interface Vector {
  x: number;
  y: number;
}

export type ViewCoordinate = Coordinate;
export type WorldCoordinate = Coordinate;

export type ViewVector = Vector;
export type WorldVector = Vector;
